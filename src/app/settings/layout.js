export const metadata = {
  title: "Settings - WIO Calculator",
  description: "Customize your WIO targets, annual leave quotas, and location settings.",
};

export default function SettingsLayout({ children }) {
  return children;
}
