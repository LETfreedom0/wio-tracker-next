我已为您实现了新的极简打卡主页。

**主要改动：**
1.  **新增 `MobileStatusView` 组件**：这是一个极简的页面，包含“在公司”和“居家”两个大按钮，以及今天的日期显示。
2.  **重构主页布局**：
    *   在移动端，采用了左右滑动的布局（Snap Scroll）。
    *   **第一屏（默认）**：新的极简打卡页。
    *   **第二屏**：原有的日历和统计页。
    *   您可以直接在第一屏点击按钮快速打卡，也可以向左滑动查看详细日历。
3.  **PC 端兼容**：在电脑宽屏上，依然保持原有的完整日历视图，不显示极简打卡页（因为 PC 端鼠标操作通常不需要这种大按钮）。

**文件修改：**
*   `src/app/components/MobileStatusView.js`: 新建文件。
*   `src/app/page.js`: 引入新组件并调整布局结构。
*   `src/app/globals.css`: 添加了隐藏滚动条的样式，提升滑动体验。

现在您可以打开应用查看效果。在手机模式下，您将首先看到打卡页面，向左滑动即可看到日历。